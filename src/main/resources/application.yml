spring:
  datasource:
    url: jdbc:postgresql://aws-0-us-west-2.pooler.supabase.com:6543/postgres?sslmode=require&prepareThreshold=0
    username: postgres.pvwgfcqylgkqoikanivs
    password: admin81122

    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      auto-commit: false

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate.connection.provider_disables_autocommit: true
      hibernate.jdbc.use_get_generated_keys: true

  sql:
    init:
      mode: never


# SMTP (ejemplo SMTP2GO)

  mail:
    host: smtp.smtp2go.com
    port: 587
    username: canelaninabombonaomora
    password: Jimena81122
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

# server port (cambiar si es necesario)
server:
  port: 8080
